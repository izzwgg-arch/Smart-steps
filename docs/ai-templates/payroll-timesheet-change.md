# Payroll / Timesheet Change Template

Use for timesheets, payroll, fingerprint/clock imports, exports, PDFs.

1. Read `CURSOR_START_HERE.md`, `PAYROLL_TIMESHEETS.md`, `DATA_MODEL.md`.
2. Confirm app boundary. In `aplus-center-scheduling`, implementation was not found in this pass.
3. Search exact terms before editing:
   - `timesheet`
   - `payroll`
   - `fingerprint`
   - `clock`
   - `batch`
   - `pdf`
   - `export`
4. Collect fixture examples for date/time calculations.
5. Do not change calculations without tests/examples.
6. Update `PAYROLL_TIMESHEETS.md`.

Risk default: high/extreme.
